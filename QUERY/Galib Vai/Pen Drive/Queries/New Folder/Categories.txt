SELECT
    ROW_ID, 
    CATEGORY_ID,
    STRUCTURE_ID,
    SEGMENT1,
    SEGMENT2,
    LAST_UPDATE_DATE,
    LAST_UPDATED_BY,
    CREATION_DATE,
    CREATED_BY,
    LAST_UPDATE_LOGIN,
    CATEGORY_CONCAT_SEGS,
    STRUCTURE_NAME,
    DESCRIPTION
FROM 
    APPS.MTL_CATEGORIES_V
WHERE
--     TRUNC (CREATION_DATE) BETWEEN '01-SEP-2015' AND '30-SEP-2015'
      CATEGORY_CONCAT_SEGS = 'AUTOMOBILE|PLUNGER'